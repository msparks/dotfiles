# Add to and unique-ify the path
path=(~/bin $path)
path=(~/.local/bin $path)
path=($path /usr/sbin /sbin /opt/bin /usr/local/sbin /usr/local/bin)
path=($path /opt/local/bin /opt/local/sbin)  # MacPorts paths
typeset -U path
